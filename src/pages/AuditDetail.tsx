import React, { useState, useEffect } from 'react';\nimport { useParams, useNavigate } from 'react-router-dom';\nimport { useAuth } from '../lib/auth';\nimport { getAudit, getFindings, createFinding, updateFinding, deleteFinding } from '../lib/queries';\nimport { Audit, Finding, FINDING_STATUSES, RISK_LEVELS } from '../lib/types';\nimport { Layout } from '../components/Layout';\nimport { Badge } from '../components/Badge';\nimport { Modal } from '../components/Modal';\nimport { Toast } from '../components/Toast';\nimport { Plus, Edit, Trash2, ChevronLeft } from 'lucide-react';\n\nexport function AuditDetail() {\n  const { id } = useParams<{ id: string }>();\n  const { user } = useAuth();\n  const navigate = useNavigate();\n  const [audit, setAudit] = useState<Audit | null>(null);\n  const [findings, setFindings] = useState<Finding[]>([]);\n  const [loading, setLoading] = useState(true);\n  const [activeTab, setActiveTab] = useState<'overview' | 'findings'>('overview');\n  const [showFindingModal, setShowFindingModal] = useState(false);\n  const [editingFinding, setEditingFinding] = useState<Finding | null>(null);\n  const [toast, setToast] = useState<{ message: string; type: 'success' | 'error' } | null>(null);\n  const [searchQuery, setSearchQuery] = useState('');\n  const [filterRisk, setFilterRisk] = useState<string>('');\n  const [filterStatus, setFilterStatus] = useState<string>('');\n\n  // Finding form state\n  const [findingForm, setFindingForm] = useState({\n    finding_no: '',\n    title: '',\n    criteria: '',\n    condition_text: '',\n    root_cause: '',\n    risk_impact: '',\n    financial_impact: '',\n    risk_rating: '',\n    recommendation: '',\n    management_response: '',\n    responsible_person: '',\n    target_date: '',\n    status: 'Open',\n  });\n\n  useEffect(() => {\n    const loadData = async () => {\n      if (!id) return;\n      try {\n        const auditData = await getAudit(id);\n        setAudit(auditData);\n\n        const findingsData = await getFindings(id);\n        setFindings(findingsData);\n      } catch (err) {\n        console.error('Error loading audit:', err);\n        setToast({ message: 'Failed to load audit', type: 'error' });\n      } finally {\n        setLoading(false);\n      }\n    };\n\n    loadData();\n  }, [id]);\n\n  const filteredFindings = findings\n    .filter((f) =>\n      f.finding_no.toLowerCase().includes(searchQuery.toLowerCase()) ||\n      f.title.toLowerCase().includes(searchQuery.toLowerCase())\n    )\n    .filter((f) => (filterRisk ? f.risk_rating === filterRisk : true))\n    .filter((f) => (filterStatus ? f.status === filterStatus : true));\n\n  const handleOpenFindingModal = (finding?: Finding) => {\n    if (finding) {\n      setEditingFinding(finding);\n      setFindingForm({\n        finding_no: finding.finding_no,\n        title: finding.title,\n        criteria: finding.criteria || '',\n        condition_text: finding.condition_text || '',\n        root_cause: finding.root_cause || '',\n        risk_impact: finding.risk_impact || '',\n        financial_impact: finding.financial_impact?.toString() || '',\n        risk_rating: finding.risk_rating || '',\n        recommendation: finding.recommendation || '',\n        management_response: finding.management_response || '',\n        responsible_person: finding.responsible_person || '',\n        target_date: finding.target_date || '',\n        status: finding.status,\n      });\n    } else {\n      setEditingFinding(null);\n      setFindingForm({\n        finding_no: '',\n        title: '',\n        criteria: '',\n        condition_text: '',\n        root_cause: '',\n        risk_impact: '',\n        financial_impact: '',\n        risk_rating: '',\n        recommendation: '',\n        management_response: '',\n        responsible_person: '',\n        target_date: '',\n        status: 'Open',\n      });\n    }\n    setShowFindingModal(true);\n  };\n\n  const handleSaveFinding = async () => {\n    if (!id || !findingForm.finding_no || !findingForm.title) {\n      setToast({ message: 'Finding No and Title are required', type: 'error' });\n      return;\n    }\n\n    try {\n      const payload = {\n        ...findingForm,\n        financial_impact: findingForm.financial_impact\n          ? parseFloat(findingForm.financial_impact)\n          : null,\n      };\n\n      if (editingFinding) {\n        await updateFinding(editingFinding.id, payload);\n        setToast({ message: 'Finding updated successfully', type: 'success' });\n      } else {\n        await createFinding({\n          ...payload,\n          audit_id: id,\n        });\n        setToast({ message: 'Finding created successfully', type: 'success' });\n      }\n\n      const updatedFindings = await getFindings(id);\n      setFindings(updatedFindings);\n      setShowFindingModal(false);\n    } catch (err) {\n      setToast({ message: 'Failed to save finding', type: 'error' });\n    }\n  };\n\n  const handleDeleteFinding = async (findingId: string) => {\n    if (!confirm('Are you sure you want to delete this finding?')) return;\n\n    try {\n      await deleteFinding(findingId);\n      setToast({ message: 'Finding deleted successfully', type: 'success' });\n      if (id) {\n        const updatedFindings = await getFindings(id);\n        setFindings(updatedFindings);\n      }\n    } catch (err) {\n      setToast({ message: 'Failed to delete finding', type: 'error' });\n    }\n  };\n\n  if (loading) {\n    return (\n      <Layout>\n        <div className=\"flex items-center justify-center min-h-screen\">\n          <div className=\"text-center\">\n            <div className=\"animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600 mx-auto\"></div>\n            <p className=\"mt-4 text-gray-600\">Loading...</p>\n          </div>\n        </div>\n      </Layout>\n    );\n  }\n\n  if (!audit) {\n    return (\n      <Layout>\n        <div className=\"p-8\">\n          <p className=\"text-gray-600\">Audit not found</p>\n        </div>\n      </Layout>\n    );\n  }\n\n  return (\n    <Layout>\n      <div className=\"p-8\">\n        <button\n          onClick={() => navigate('/audits')}\n          className=\"flex items-center gap-2 text-blue-600 hover:text-blue-700 mb-6\"\n        >\n          <ChevronLeft size={20} />\n          Back to Audits\n        </button>\n\n        <div className=\"bg-white rounded-lg shadow p-8 mb-8\">\n          <h1 className=\"text-3xl font-bold text-gray-900\">{audit.title}</h1>\n          <p className=\"text-gray-600 mt-2\">{audit.audit_no}</p>\n\n          <div className=\"grid grid-cols-1 md:grid-cols-3 gap-6 mt-8\">\n            <InfoField label=\"Company\" value={audit.company || 'N/A'} />\n            <InfoField label=\"Department\" value={audit.department || 'N/A'} />\n            <InfoField label=\"Audit Type\" value={audit.audit_type || 'N/A'} />\n            <InfoField label=\"Auditor\" value={audit.auditor || 'N/A'} />\n            <InfoField label=\"Start Date\" value={audit.start_date || 'N/A'} />\n            <InfoField label=\"End Date\" value={audit.end_date || 'N/A'} />\n            <InfoField label=\"Audit Period\" value={audit.audit_period || 'N/A'} />\n            <InfoField label=\"Risk Level\" value={audit.risk_level || 'N/A'} />\n            <div>\n              <p className=\"text-gray-600 text-sm font-medium\">Status</p>\n              <div className=\"mt-2\">\n                <Badge text={audit.status} color={getStatusColor(audit.status)} />\n              </div>\n            </div>\n          </div>\n\n          {audit.objective && (\n            <div className=\"mt-6 pt-6 border-t border-gray-200\">\n              <p className=\"text-gray-600 text-sm font-medium\">Objective</p>\n              <p className=\"text-gray-900 mt-2\">{audit.objective}</p>\n            </div>\n          )}\n\n          {audit.scope && (\n            <div className=\"mt-6\">\n              <p className=\"text-gray-600 text-sm font-medium\">Scope</p>\n              <p className=\"text-gray-900 mt-2\">{audit.scope}</p>\n            </div>\n          )}\n        </div>\n\n        {/* Tabs */}\n        <div className=\"flex gap-4 mb-8 border-b border-gray-200\">\n          <button\n            onClick={() => setActiveTab('overview')}\n            className={`px-4 py-2 font-medium border-b-2 transition-colors ${\n              activeTab === 'overview'\n                ? 'border-blue-600 text-blue-600'\n                : 'border-transparent text-gray-600 hover:text-gray-900'\n            }`}\n          >\n            Overview\n          </button>\n          <button\n            onClick={() => setActiveTab('findings')}\n            className={`px-4 py-2 font-medium border-b-2 transition-colors ${\n              activeTab === 'findings'\n                ? 'border-blue-600 text-blue-600'\n                : 'border-transparent text-gray-600 hover:text-gray-900'\n            }`}\n          >\n            Findings ({findings.length})\n          </button>\n        </div>\n\n        {/* Findings Tab */}\n        {activeTab === 'findings' && (\n          <div className=\"bg-white rounded-lg shadow\">\n            <div className=\"p-6 border-b border-gray-200\">\n              <div className=\"flex items-center justify-between mb-6\">\n                <h2 className=\"text-xl font-bold text-gray-900\">Findings</h2>\n                <button\n                  onClick={() => handleOpenFindingModal()}\n                  className=\"flex items-center gap-2 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700\"\n                >\n                  <Plus size={20} />\n                  Add Finding\n                </button>\n              </div>\n\n              {/* Search and Filters */}\n              <div className=\"grid grid-cols-1 md:grid-cols-3 gap-4\">\n                <input\n                  type=\"text\"\n                  placeholder=\"Search by Finding No or Title\"\n                  value={searchQuery}\n                  onChange={(e) => setSearchQuery(e.target.value)}\n                  className=\"px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500\"\n                />\n                <select\n                  value={filterRisk}\n                  onChange={(e) => setFilterRisk(e.target.value)}\n                  className=\"px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500\"\n                >\n                  <option value=\"\">All Risk Levels</option>\n                  {RISK_LEVELS.map((risk) => (\n                    <option key={risk} value={risk}>\n                      {risk}\n                    </option>\n                  ))}\n                </select>\n                <select\n                  value={filterStatus}\n                  onChange={(e) => setFilterStatus(e.target.value)}\n                  className=\"px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500\"\n                >\n                  <option value=\"\">All Statuses</option>\n                  {FINDING_STATUSES.map((status) => (\n                    <option key={status} value={status}>\n                      {status}\n                    </option>\n                  ))}\n                </select>\n              </div>\n            </div>\n\n            {/* Findings Table */}\n            <div className=\"overflow-x-auto\">\n              <table className=\"w-full\">\n                <thead className=\"bg-gray-50 border-b border-gray-200\">\n                  <tr>\n                    <th className=\"px-6 py-3 text-left text-xs font-medium text-gray-600 uppercase tracking-wider\">\n                      Finding No\n                    </th>\n                    <th className=\"px-6 py-3 text-left text-xs font-medium text-gray-600 uppercase tracking-wider\">\n                      Title\n                    </th>\n                    <th className=\"px-6 py-3 text-left text-xs font-medium text-gray-600 uppercase tracking-wider\">\n                      Risk\n                    </th>\n                    <th className=\"px-6 py-3 text-left text-xs font-medium text-gray-600 uppercase tracking-wider\">\n                      Financial Impact\n                    </th>\n                    <th className=\"px-6 py-3 text-left text-xs font-medium text-gray-600 uppercase tracking-wider\">\n                      Status\n                    </th>\n                    <th className=\"px-6 py-3 text-left text-xs font-medium text-gray-600 uppercase tracking-wider\">\n                      Actions\n                    </th>\n                  </tr>\n                </thead>\n                <tbody className=\"divide-y divide-gray-200\">\n                  {filteredFindings.map((finding) => {\n                    const isOverdue =\n                      finding.status !== 'Closed' &&\n                      finding.target_date &&\n                      new Date(finding.target_date) < new Date();\n                    return (\n                      <tr key={finding.id} className=\"hover:bg-gray-50\">\n                        <td className=\"px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900\">\n                          {finding.finding_no}\n                        </td>\n                        <td className=\"px-6 py-4 text-sm text-gray-900\">{finding.title}</td>\n                        <td className=\"px-6 py-4 whitespace-nowrap text-sm\">\n                          <Badge\n                            text={finding.risk_rating || 'N/A'}\n                            color={getRiskColor(finding.risk_rating)}\n                          />\n                        </td>\n                        <td className=\"px-6 py-4 whitespace-nowrap text-sm text-gray-900\">\n                          {finding.financial_impact\n                            ? `BDT ${finding.financial_impact.toLocaleString()}`\n                            : '-'}\n                        </td>\n                        <td className=\"px-6 py-4 whitespace-nowrap text-sm\">\n                          <div className=\"flex items-center gap-2\">\n                            <Badge\n                              text={finding.status}\n                              color={getStatusColor(finding.status)}\n                            />\n                            {isOverdue && (\n                              <Badge text=\"OVERDUE\" color=\"red\" />\n                            )}\n                          </div>\n                        </td>\n                        <td className=\"px-6 py-4 whitespace-nowrap text-sm\">\n                          <div className=\"flex items-center gap-2\">\n                            <button\n                              onClick={() => handleOpenFindingModal(finding)}\n                              className=\"text-blue-600 hover:text-blue-700\"\n                            >\n                              <Edit size={18} />\n                            </button>\n                            <button\n                              onClick={() => handleDeleteFinding(finding.id)}\n                              className=\"text-red-600 hover:text-red-700\"\n                            >\n                              <Trash2 size={18} />\n                            </button>\n                          </div>\n                        </td>\n                      </tr>\n                    );\n                  })}\n                </tbody>\n              </table>\n            </div>\n          </div>\n        )}\n      </div>\n\n      {/* Finding Modal */}\n      <Modal\n        isOpen={showFindingModal}\n        onClose={() => setShowFindingModal(false)}\n        title={editingFinding ? 'Edit Finding' : 'Add Finding'}\n        footer={\n          <>\n            <button\n              onClick={() => setShowFindingModal(false)}\n              className=\"px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50\"\n            >\n              Cancel\n            </button>\n            <button\n              onClick={handleSaveFinding}\n              className=\"px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700\"\n            >\n              Save Finding\n            </button>\n          </>\n        }\n      >\n        <div className=\"space-y-4\">\n          <div className=\"grid grid-cols-2 gap-4\">\n            <input\n              type=\"text\"\n              placeholder=\"Finding No *\"\n              value={findingForm.finding_no}\n              onChange={(e) => setFindingForm({ ...findingForm, finding_no: e.target.value })}\n              className=\"px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500\"\n              required\n            />\n            <input\n              type=\"text\"\n              placeholder=\"Title *\"\n              value={findingForm.title}\n              onChange={(e) => setFindingForm({ ...findingForm, title: e.target.value })}\n              className=\"px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500\"\n              required\n            />\n          </div>\n\n          <input\n            type=\"text\"\n            placeholder=\"Criteria\"\n            value={findingForm.criteria}\n            onChange={(e) => setFindingForm({ ...findingForm, criteria: e.target.value })}\n            className=\"w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500\"\n          />\n\n          <input\n            type=\"text\"\n            placeholder=\"Condition / Finding\"\n            value={findingForm.condition_text}\n            onChange={(e) => setFindingForm({ ...findingForm, condition_text: e.target.value })}\n            className=\"w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500\"\n          />\n\n          <input\n            type=\"text\"\n            placeholder=\"Root Cause\"\n            value={findingForm.root_cause}\n            onChange={(e) => setFindingForm({ ...findingForm, root_cause: e.target.value })}\n            className=\"w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500\"\n          />\n\n          <input\n            type=\"text\"\n            placeholder=\"Risk / Impact\"\n            value={findingForm.risk_impact}\n            onChange={(e) => setFindingForm({ ...findingForm, risk_impact: e.target.value })}\n            className=\"w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500\"\n          />\n\n          <input\n            type=\"number\"\n            placeholder=\"Financial Impact (BDT)\"\n            value={findingForm.financial_impact}\n            onChange={(e) => setFindingForm({ ...findingForm, financial_impact: e.target.value })}\n            className=\"w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500\"\n          />\n\n          <select\n            value={findingForm.risk_rating}\n            onChange={(e) => setFindingForm({ ...findingForm, risk_rating: e.target.value })}\n            className=\"w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500\"\n          >\n            <option value=\"\">Select Risk Rating</option>\n            {RISK_LEVELS.map((risk) => (\n              <option key={risk} value={risk}>\n                {risk}\n              </option>\n            ))}\n          </select>\n\n          <input\n            type=\"text\"\n            placeholder=\"Recommendation\"\n            value={findingForm.recommendation}\n            onChange={(e) => setFindingForm({ ...findingForm, recommendation: e.target.value })}\n            className=\"w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500\"\n          />\n\n          <input\n            type=\"text\"\n            placeholder=\"Management Response\"\n            value={findingForm.management_response}\n            onChange={(e) => setFindingForm({ ...findingForm, management_response: e.target.value })}\n            className=\"w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500\"\n          />\n\n          <input\n            type=\"text\"\n            placeholder=\"Responsible Person\"\n            value={findingForm.responsible_person}\n            onChange={(e) => setFindingForm({ ...findingForm, responsible_person: e.target.value })}\n            className=\"w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500\"\n          />\n\n          <input\n            type=\"date\"\n            value={findingForm.target_date}\n            onChange={(e) => setFindingForm({ ...findingForm, target_date: e.target.value })}\n            className=\"w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500\"\n          />\n\n          <select\n            value={findingForm.status}\n            onChange={(e) => setFindingForm({ ...findingForm, status: e.target.value })}\n            className=\"w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500\"\n          >\n            {FINDING_STATUSES.map((status) => (\n              <option key={status} value={status}>\n                {status}\n              </option>\n            ))}\n          </select>\n        </div>\n      </Modal>\n\n      {toast && (\n        <Toast\n          message={toast.message}\n          type={toast.type}\n          onClose={() => setToast(null)}\n        />\n      )}\n    </Layout>\n  );\n}\n\nfunction InfoField({ label, value }: { label: string; value: string }) {\n  return (\n    <div>\n      <p className=\"text-gray-600 text-sm font-medium\">{label}</p>\n      <p className=\"text-gray-900 mt-1\">{value}</p>\n    </div>\n  );\n}\n\nfunction getStatusColor(\n  status: string\n): 'red' | 'yellow' | 'green' | 'blue' | 'gray' {\n  switch (status) {\n    case 'Completed':\n      return 'green';\n    case 'Ongoing':\n      return 'blue';\n    case 'Planned':\n      return 'yellow';\n    case 'On Hold':\n      return 'red';\n    case 'Open':\n      return 'red';\n    case 'Management Response Pending':\n      return 'yellow';\n    case 'In Progress':\n      return 'blue';\n    case 'Closed':\n      return 'green';\n    default:\n      return 'gray';\n  }\n}\n\nfunction getRiskColor(\n  risk?: string\n): 'red' | 'yellow' | 'green' | 'blue' | 'gray' {\n  switch (risk) {\n    case 'High':\n      return 'red';\n    case 'Medium':\n      return 'yellow';\n    case 'Low':\n      return 'green';\n    default:\n      return 'gray';\n  }\n}\n